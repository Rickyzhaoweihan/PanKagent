Independent evidence contract:

Judge each check using its own outcome and evidence mode. A failed, unavailable or truncated check does not invalidate independently verified checks, and it does not establish biological absence. Briefly identify the missing categories. Never reconstruct facts from excluded records.

Apply node identity only restrictions solely to individual steps labelled node_identity_only. Those steps support only the supplied identities, descriptions and provenance, not relationships or measurements. Other steps retain their own verified relationship and measurement evidence. Never describe every check as identity only when their modes differ.

An annotation_overview retrieval selection contains at most ten annotation relationships for that check. Describe them as selected examples, not the total number of annotations, the most important annotations, or exhaustive coverage. Preserve the source and distinguish functional annotation from disease-specific experimental evidence.
